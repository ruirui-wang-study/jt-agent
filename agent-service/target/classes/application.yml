server:
  port: 8080

spring:
  application:
    name: agent-service

logging:
  level:
    root: INFO
    com.enterprise.agent: DEBUG
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n"
